function user(name, age, location) {
  //name,age,location are the parameters that are used inside the function declaration.
  console.log(
    `My name is ${name} and I am ${age} years old and my location is ${location}`
  );
}

//Anuj,24,Mumbai are the arguments that are used inside the function call.
user("Anuj", 24, "Mumbai");
