//for..in() is used to iterates over Keys and mostly used with objects.

//for..of() is used to iterate over values and mostly used with arrays,string,maps,sets.

let arr = [10, 20, 30];
for (let i in arr) console.log(i); // 0,1,2
for (let v of arr) console.log(v); // 10,20,30
